import {useRef,useState} from "react";

import { Form } from "./Form";
 




function Hero({children}){
   
    
   
    return(
        <div className="hero">
           {children}
           
        </div>
    )
}

export default Hero;
